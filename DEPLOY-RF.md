# Переезд ЛУЧ-ИИ с Vercel на РФ-хостинг (Timeweb Cloud Apps)

Цель: сайт открывается из РФ без VPN, ПД остаётся в РФ-контуре, живые модули
(чат-консультант, SMM) работают на DeepSeek, SEO-позиции не теряются.

## Что уже сделано в коде (ветка `migrate-rf-hosting`)

- `output: "standalone"` + `outputFileTracingRoot` → `Dockerfile` собирает лёгкий образ.
- `X-Accel-Buffering: no` на `/api/consult` — стриминг чата не буферизуется прокси.
- Заявки: полная заявка (ПД) → письмом на РФ-ящик (`lib/mailer.ts`), в Telegram —
  только пинг без ПД (`lib/notify.ts`). `/api/lead` и `/api/consult` переписаны.
- В форме поле «Имя» заменено на «Компания или ЖК» (`org`) — не ПД.
- В чате телефоны/email маскируются перед отправкой в LLM.

## Шаг 1. Российский почтовый ящик + пароль приложения

1. Завести ящик на Yandex 360 (или Mail.ru для бизнеса) на своём домене/бесплатный.
2. Включить доступ по SMTP и создать **пароль приложения** (не основной пароль).
3. Значения для env: `SMTP_HOST=smtp.yandex.ru`, `SMTP_PORT=465`, `SMTP_USER=<ящик>`,
   `SMTP_PASS=<пароль приложения>`, `LEAD_TO=<куда слать заявки>`.

## Шаг 2. Timeweb Cloud Apps

1. Создать аккаунт Timeweb Cloud → «Приложения» (Apps) → «Создать из GitHub».
2. Указать репозиторий `photozyxer/luch-ii`, ветку (после мержа — `main`).
3. Тип сборки — **Dockerfile** (он в корне `luch/`). Порт приложения — **3000**.
4. Прописать переменные окружения (панель Timeweb, НЕ в коде):
   `SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, LEAD_TO, DEEPSEEK_API_KEY`,
   опционально `TG_BOT_TOKEN, TG_CHAT_ID` (для пинга), `LLM_DAILY_CAP`,
   `UPSTASH_REDIS_REST_URL/TOKEN` (для durable-лимитов).
   `OPENAI_API_KEY` можно НЕ задавать — с РФ-сервера OpenAI недоступен.
5. Дождаться сборки, получить временный адрес Timeweb (`*.twc1.net` или похожий).

## Шаг 3. ПРОВЕРКА на временном адресе (до переключения домена!)

Открыть временный адрес и проверить:
- [ ] Главная и все страницы отдаются (200).
- [ ] `/consultant` — чат отвечает и **стримит** текст → DeepSeek доступен с РФ-сервера.
      ⚠️ Если чат молчит/ошибка 502 — DeepSeek с РФ-IP недоступен. План Б: подключить
      YandexGPT/GigaChat (РФ-нативные, заодно закрывают ПД в чате).
- [ ] `/smm` — генерация карусели/поста работает.
- [ ] Отправить тестовую заявку → **письмо пришло** на РФ-ящик; в Telegram — пинг
      **без телефона**.
- [ ] Логи Timeweb без ошибок.

## Шаг 4. Переключение домена (SEO-безопасный cutover)

1. **Заранее** снизить TTL A-записей `luch-ii.ru` до 300 c (в текущем DNS).
2. Держать Vercel живым до конца.
3. Привязать домен `luch-ii.ru` (и `www`) к приложению Timeweb, дождаться выпуска
   SSL (Let's Encrypt автоматически).
4. Переключить A/ALIAS-записи на адрес Timeweb.
5. Проверить: сайт открывается из РФ **без VPN** (телефон по мобильной сети, без VPN,
   или сервис проверки доступности из РФ). HTTPS валиден, `www` → apex редиректит.
6. Через 1–2 недели, убедившись в стабильности, отключить проект на Vercel.

## Шаг 5. Не потерять SEO

- Домен и все URL не меняются → ранжирование сохраняется.
- Яндекс.Вебмастер: подтвердить сайт (если ещё нет), пересабмитить `sitemap.xml`,
  проверить «Переобход». РФ-хостинг + доступность + скорость — плюс для Яндекса.
- Google Search Console: пересабмитить `sitemap.xml`, «Проверка URL» для главных.
- Проверить, что `robots.txt` и `sitemap.xml` отдают `https://luch-ii.ru` (уже так).

## Шаг 6. Юридическое (вне кода, зона владельца)

- Подать в Роскомнадзор уведомление об обработке ПД (оператор — НИКС/юрлицо).
- Проверить, что `/privacy` актуальна: ПД хранятся/обрабатываются в РФ, цели, сроки.
- Согласие на обработку ПД в форме уже есть (чекбокс + ссылка на политику).

## Оценка стоимости

- Timeweb Cloud Apps: от нескольких сотен ₽/мес.
- Домен уже есть, SSL бесплатный (Let's Encrypt).
- DeepSeek — оплата по факту (дёшево), дневной потолок `LLM_DAILY_CAP` от накрутки.
